# LegadoHub Candidate Fixes

| ID | Finding | Proposed change | Risk if unchanged | Approval required | Status |
| --- | --- | --- | --- | --- | --- |
| CF-01 | `backend/app/main.py` serves `/app/frontend/index.html` from the root route, which is invalid outside the container layout | Decide whether local development should proxy to Vite, serve `frontend/dist`, or return a setup hint when no built SPA is present | Local root path fails even when API is healthy | Yes | Confirmed - awaiting approval |
| CF-02 | `backend/app/core/config.py` ships with a hard-coded default `SECRET_KEY` | Require an explicit secret for non-test environments or generate a per-instance dev secret automatically | JWT trust and password hashing both rely on a known default if operators forget to override it | Yes | Confirmed - awaiting approval |
| CF-03 | `backend/app/main.py` hard-codes wildcard CORS with credentials instead of using configuration | Route CORS through `settings.CORS_ORIGINS` and ship restrictive defaults outside local development | Browser-origin trust stays broader than necessary | Yes | Confirmed - awaiting approval |
| CF-04 | Runtime only exposes v0 routers while docs still advertise v1/auth/AI/websocket/translate surfaces | Either enable the intended v1 routers or downgrade docs/tests/UI copy to match the actual runtime surface | Docs/runtime mismatch misleads maintainers and operators during takeover | Yes | Confirmed - awaiting approval |
| CF-05 | Local backend startup on Python 3.13 is not reproducible from `requirements.txt`, and the default DB path is Docker-centric | Normalize supported Python/dependency pins, add missing runtime packages, and replace `/data/legado_hub.db` with a documented local-friendly behavior | New maintainers cannot reliably reproduce the runtime without ad-hoc environment surgery | Yes | Confirmed - awaiting approval |
| CF-06 | Frontend and backend no longer agree on query parameter casing or pagination response shape | Standardize on one contract (`page_size`/`enabled_only` vs `pageSize`/`enabledOnly`, and `meta.total` vs top-level fields) and update all affected pages | Filtering, pagination, export toggles, and search/test source loading remain inconsistent or silently wrong | Yes | Confirmed - awaiting approval |
| CF-07 | `dashboard.py` computes stats from `page_size=1` slices and returns empty groups when `list_groups()` is missing | Use repository totals instead of `len(items)` from one-page slices and implement a real groups path instead of silent fallback | Dashboard counts remain incorrect and group-based views cannot become trustworthy | Yes | Confirmed - awaiting approval |
| CF-08 | Auth hardening is incomplete: v0 writes are unauthenticated and stored passwords use custom `SHA256 + SECRET_KEY` hashing | Decide whether v0 write/import routes must require auth, then gate them accordingly and replace password hashing with bcrypt/argon2 | Any reachable deployment keeps a weak admin credential story and an unauthenticated mutable v0 data plane | Yes | Confirmed - awaiting approval |
